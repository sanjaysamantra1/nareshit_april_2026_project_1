import { DisablePaste } from './disable-paste';

describe('DisablePaste', () => {
  it('should create an instance', () => {
    const directive = new DisablePaste();
    expect(directive).toBeTruthy();
  });
});
